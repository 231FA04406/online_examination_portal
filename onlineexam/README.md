# Online Examination Portal

This workspace will host both frontend (client) and backend (server). Currently, the frontend is scaffolded in `client/`.

## Frontend
See `client/README.md` for instructions.
